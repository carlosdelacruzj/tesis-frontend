export const environment = {
  production: false,
  // Si usas proxy (recomendado), usa base relativa
  baseUrl: 'http://localhost:8080/api/v1'
  // Si NO usas proxy, apunta directo:
  // baseUrl: 'http://localhost:8080/api/v1'
};

